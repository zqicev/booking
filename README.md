.env structure:
```
DB_HOST - database host
DB_USER - database user
DB_PASS - database password
DB_NAME - database name
DB_PORT - database port
```

Application using PostgreSQL

Before starting use "npm i" for install all node packets in pacakge.json file
You can change server's port in config.js file

Структура .env:
```
DB_HOST - Хост бд
DB_USER - Пользователь бд
DB_PASS - Пароль бд
DB_NAME - Имя бд
DB_PORT - Порт бд
```

Приложение использует PostgreSQL

Перед запуском используйте "npm i", чтобы установить все необходимые пакеты node.js, указанные в файле package.json
Вы можете изменить порт сервера в файле config.js